algoritmo "ex66"

var
contador, valor, resultados: inteiro

inicio

escreval("Digite o valor da tabuada que deseja exibir: ")
leia(valor)

Para contador <- 1 ate 10 faça
     resultados <- valor * contador
     escreval(valor, " X ", contador, " = ", resultados)
FimPara

fimalgoritmo